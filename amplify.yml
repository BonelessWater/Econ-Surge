version: 1
frontend:
    phases:
        build:
            commands:
                - 'echo "frontend deployment"'
    artifacts:
        baseDirectory: /
        files:
            - '**/*'
    cache:
        paths: []
